import React from 'react';
import { Box, Text } from 'react-native-magnus';

// export const LogIn = ({navigation, route}: LogInScreenProps) => {
export const LogIn = () => {
  return (
    <Box flex={1} justifyContent="center" alignItems="center">
      <Text>Log In Screen</Text>
    </Box>
  );
};
